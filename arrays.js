'use strict';

let days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday','Thursday', 'Friday', 'Saturday'];

// console.log(days[6]);

for(let i = 0; i < days.length; i++){
    console.log(days[i]);
}

let heights = [1.7, 1.65, 1.5, 1.9];
console.log(heights.length);
console.log(heights[2]);